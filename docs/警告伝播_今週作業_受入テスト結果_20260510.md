# 警告伝播・今週作業 受入テスト結果

- 作成日: 2026-05-10 15:48 JST
- 作成者: Codex (GPT-5)
- 更新日: 2026-05-10

## 対象

今回追加・修正した次の仕様を対象に確認した。

- 下位タスクの `!!` / `!` を上位タスクへ伝播する
- `!!` と `!` が混在する場合は `!!` を優先する
- `!` は予定期間が今週と重なる未完了タスクとして判定する
- `今週作業` シートで `進行中`、`未着手かつ遅延`、`今週するはずの未着手` をカテゴリ順に抽出する
- Excel シリアル値として入った予定日も、警告と今週作業の判定対象にする

## 実施結果

| 確認 | 対象 | 結果 | 証跡 |
|---|---|---|---|
| 最小診断 | v3 | OK | `weekly_diagnostic_after_date_fix.xlsm` にて、行 975-980 の `!!` / `!` と `今週作業` 抽出を確認 |
| 受入テスト | v3 / Lite | OK | `output/acceptance_test_20260510_154524/acceptance-results.md`、OK=38、NG=0 |
| 配布物・復元後テスト | v3 / Lite / v3-restored / Lite-restored | OK | `output/acceptance_test_20260510_155840/acceptance-results.md`、OK=78、NG=0 |
| 最終配布物・復元後テスト | v3 / Lite / v3-restored / Lite-restored | OK | `output/acceptance_test_20260510_161345/acceptance-results.md`、OK=78、NG=0 |

## 確認した主な観点

1. LV3 の遅延 `!!` が LV2 / LV1 へ伝播すること。
2. 今週予定の `!` が上位タスクへ伝播すること。
3. `!!` がある配下では上位タスクに `!` ではなく `!!` が表示されること。
4. `今週作業` シートで、進行中、遅延未着手、今週未着手が順番通り出ること。
5. 来週以降のみの未着手タスクと完了済みタスクが `今週作業` に出ないこと。
6. 予定日が Excel 内部数値で入っていても、日付として判定されること。
7. `今週作業` 作成時に自動テストモードでは完了ダイアログで停止しないこと。

## 残リスク

- `今週作業` は既存の同名シートを作り直す仕様のため、利用者が同シートに手入力した内容は再生成時に消える。運用上は出力専用シートとして扱う。
- 予定日が極端に古い数値や日付として不正な文字列の場合は、通常どおり日付なしとして扱う。
